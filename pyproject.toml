[project]
name = "cc-course-finder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pypdf>=6.10.0",
    "requests>=2.33.1",
    "typer>=0.24.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
